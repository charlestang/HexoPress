## Implementation Tasks
- [x] 新增 `MediaLibraryView` 页面，含图片/文件标签页、卡片网格、空态。
- [x] 实现 `buildImageGroups` 工具函数，支持 `-WxH` 归一化分组。
- [x] 新增 `MediaDetailView` 页面，含代表图预览、元数据、变体表格。
- [x] 实现删除流程：确认弹窗、loading 状态、成功/失败反馈、刷新列表。
- [x] 扩展 `FsAgent` 提供 `getFileInfo` 和 `findAssetReferences` 接口。
- [x] 详情页无效参数自动重定向回媒体库列表。
- [x] 新增中英文 i18n 文案。
- [x] 为 `buildImageGroups` 编写单元测试。
