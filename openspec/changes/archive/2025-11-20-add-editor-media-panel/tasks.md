- [x] 1. 在 FrameView toolbar 中注册 mediaPanel（同步 fileTree/tocPanel 的机制），并以倒序方式加载全部图片缩略图（列数自适应、可滚动、每次展开调用 `getAssets`）。
- [x] 2. 封装可复用的 SearchBar 组件（输入框、命中计数、上下跳转），在 mediaPanel 中实现关键字搜索（前端过滤、大小写不敏感、空输入不触发）、命中计数与高亮。
- [x] 3. 单击缩略图弹出放大预览对话框（显示路径/尺寸/大小/创建时间），提供“插入/关闭”操作；双击缩略图直接插入并关闭对话框。
- [x] 4. 实现 Markdown 插入逻辑：优先使用正文光标位置，若不在正文则追加至正文末尾并换行；生成 `![](relative-path)`（路径按文章 permalink 计算指向网站根下 `images/`），插入成功后保持焦点并关闭对话框。
- [x] 5. 增补必要的单元测试/集成测试（如 SearchBar 行为、搜索跳转、插入调用、上传后刷新 mediaPanel）并更新文档/类型。
